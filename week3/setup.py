from setuptools import setup, find_packages

setup(
   author='Sarratus',
   name='package',
   version='1.0',
   description='A useful module',
   packages=find_packages(),
   install_requires='',
)
